import eventList from './mockEvent'
import locationList from './mockLocation'
import timeList from './mockTime'

const dataSearch = {
  event: eventList,
  location: locationList,
  time: timeList
}

export default dataSearch
